//알파벳 소문자로만 이루어진 단어 S가 주어진다.
// 각 알파벳이 단어에 몇 개가 포함되어 있는지 구하는 프로그램을 작성하시오.

//단어에 포함되어 있는 a의 개수, b의 개수, …, z의 개수를
// 공백으로 구분해서 출력한다.

function solution(a) {
  // 크기가 26인 배열을 생성하고 모든 값을 0으로 초기화. 이 배열의 각 인덱스는 알파벳 'a'부터 'z'까지
  let arr = 

  // 문자열 S를 한글자씩 순회 해당 문자 아스키 코드 값 이용해서 배열 인덱스 찾아 값 증가
  // charCodeAt() 메서드 'a.charCodeAt(0)'
  // ch.charCodeAt(0) - 'a'.charCodeAt(0)
  return a;
}
console.log(solution(1));
