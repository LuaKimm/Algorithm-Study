const arr = ["Hello", "World", "!"];
const [first, second] = arr;
console.log(first, second);

const obj = { name: "이선협", publisher: "골든래빗" };
const { name, publisher } = obj;
console.log(name, publisher);

//객체의 구조 분해 할당 중괄호를 사용한다.
