// 10 + 20은 평가되어 숫자 값 30을 생성한다.
10 + 20; // 30

// 변수에는 10 + 20이 평가되어 생성된 숫자 값 30이 할당된다.
var sum = 10 + 20;

// 숫자 리터럴 3
3;

var score = 100;

var score = 50 + 50;

score; // -> 100

// 함수 / 메서드 호출 표현식(선언이 이미 존재한다고 가정)
// square();
// person.getName();

var x = 1 + 2;
x + 3; //6
